AppBundle\Entity\Customer:
    exclusion_policy: ALL
    properties:
        id:
            expose: true
            type: integer
            groups: [read, write]
        firstName:
            expose: true
            type: string
            groups: [read, write]
        lastName:
            expose: true
            type: string
            groups: [read, write]
        user:
            expose: true
            type: AppBundle\Entity\User
            groups: [read, write]