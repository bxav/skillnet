{% extends 'layout.html.twig' %}

{% block content %}

    <!-- main-container start -->
    <!-- ================ -->
    <section class="main-container">

        <div class="container">
            <div class="row">

                <!-- main start -->
                <!-- ================ -->
                <div class="main col-md-12">

                    <!-- page-title start -->
                    <!-- ================ -->
                    <h1 class="page-title">{{ 'customer.my_bookings' | trans }}</h1>
                    <div class="separator-2"></div>
                    <!-- page-title end -->

                    <table class="table cart table-hover table-colored">
                        <thead>
                        <tr>
                            <th>{{ 'customer.my_bookings.service' | trans }} </th>
                            <th>{{ 'customer.my_bookings.date' | trans }} </th>
                            <th>{{ 'cancel' | trans }} </th>
                            <th class="amount">{{ 'price' | trans }} </th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for booking in bookings %}
                        <tr class="remove-data">
                            <td class="product"><a href="#">{{ booking.service.type }}</a></td>
                            <td class="datetime">{{ booking.startDatetime | date }}</td>
                            <td class="remove"><a class="btn btn-remove btn-sm btn-default">Annuler</a></td>
                            <td class="amount">{{ booking.service.price }} </td>
                        </tr>
                        {% else %}
                        <tr>
                            <td colspan="4">{{ 'customer.my_bookings.no_booking' | trans }}</td>
                        </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
                <!-- main end -->

            </div>
        </div>
    </section>
    <!-- main-container end -->



{% endblock %}