{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">

        <div class="starter-template">
            <h1>Bootstrap starter template</h1>
            {% for business in businesses%}
                <p class="lead">{{ business.slug }}</p>
            {% endfor %}

        </div>

    </div><!-- /.container -->
{% endblock %}
